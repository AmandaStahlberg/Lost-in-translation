const TranslationButton = ({name, image}) => {
    return (
        <button>
            <section>
                <b>{ name }</b>
            </section>
        </button>
    )
}
export default TranslationButton